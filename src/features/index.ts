/**
 * Features index - Central export point for all feature modules
 * Each feature is self-contained with its own components and logic
 */

// Catalog & Shopping
export { CatalogView } from "./catalog";

// Cart & Checkout
export { CartView } from "./cart";

// Wishlist
export { WishlistView } from "./wishlist";

// Search
export { SearchView } from "./search";

// Swatches
export { SwatchesView } from "./swatches";

// Product Detail, Style Finder, Inspiration, Consultation, Quote, Account
// (These are primarily route-based features with minimal extracted components)
